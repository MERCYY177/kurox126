name: 手动站点完整性检查

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 检查站点入口
        run: test -s index.html
      - name: 检查数据文件
        run: test -s data/items.js
